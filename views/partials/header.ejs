<!DOCTYPE html>
<html>
    <head>
        <title>Gym Connect</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/">GymConnect</a>
                <div class="navbar-collapse">
                    <div class="navbar-nav">
                        <a class="nav-link" href="/gyms">Home</a>
                        <a class="nav-link" href="/users">Members</a>
                    </div>
                </div>
                <div class="mr-auto">
                    <div class="navbar-nav">
                        <% if(!currentUser) { %>
                            <a class="nav-link" href="/login">Login</a>
                            <a class="nav-link" href="/register">Sign Up</a>
                        <% } else { %>
                            <a class="nav-link active" href="/users/<%= currentUser._id %>"><%= currentUser.username %></a>
                            <a class="nav-link" href="/logout">Log Out</a>
                        <% } %>
                    </div>
                </div>
            </div>
        </nav>
        
        <div class="container mb-5">
            <% if(error && error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length > 0) { %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>